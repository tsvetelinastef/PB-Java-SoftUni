package PreparationForExam;

import java.util.Scanner;

public class Ex5 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

      //  int sea = Integer.parseInt(scanner.nextLine());
      //  int mountain = Integer.parseInt(scanner.nextLine());


/*
        int goalForDay = Integer.parseInt(scanner.nextLine());

        int income = 0; //  приходът


        String command = scanner.nextLine();
        if (command.equals("haircut") || command.equals("colour")) {
            String typeHaircut = scanner.nextLine();
            if (typeHaircut.equals("mens")) {
                income += 15;
            } else if (typeHaircut.equals("ladies")) {
                income += 20;
            } else if (typeHaircut.equals("kids")) {
                income += 10;
            }
            String typeColour = scanner.nextLine();
            if (typeColour.equals("touch up")){
                income += 20;
            } else if(typeColour.equals("full colour")) {
                income += 30;
            }
        }

        /*
        if (command.equals("colour")) {

            String typeColour = scanner.nextLine();
            if (typeColour.equals("touch up")){
                income += 20;
            } else if(typeColour.equals("full colour")) {
                income += 30;
            }
        }

         */

        /*

        if (command.equals("closed")) {
            System.out.println();
        }

        if (goalForDay < income){
            System.out.println("You have reached your target for the day!");
            System.out.printf("Earned money: %dlv.%n", income);
        } else {
            System.out.printf("Target not reached! You need %dlv. more.", goalForDay - income);
            System.out.printf("Earned money: %dlv.%n", income);
        }


         */


    }
}
